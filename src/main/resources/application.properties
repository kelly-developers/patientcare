# Server Configuration
server.port=8080
server.servlet.context-path=/api

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/patientcare
spring.datasource.username=postgres
spring.datasource.password=password

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration
app.jwt.secret=F2oqDCfF28jVtYT8LKzPovFVvZSb5tTAnR5POXrmUQU=
app.jwt.expiration=900000
app.jwt.refresh-expiration=604800000

# CORS
app.cors.allowed-origins=https://patientcares.netlify.app,http://localhost:3000

# Logging
logging.level.com.example.patientcare=DEBUG
logging.level.org.springframework.security=DEBUG